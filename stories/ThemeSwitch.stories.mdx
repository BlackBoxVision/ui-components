import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Button } from '@material-ui/core';

import { addThemeProvider } from './utils/decorators/theme';
import { useDarkMode } from '../src/hooks/useDarkMode';
import { ThemeSwitch, theme } from '../src';

<Meta title="UI Components/ThemeSwitch" component={ThemeSwitch} />

# ThemeSwitch

Stories related to **ThemeSwitch** component.

## Example

This is how you can use **ThemeSwitch** component:

```jsx
import React from 'react';
import { ThemeSwitch } from '@blackbox-vision/ui-components';

export const MyThemeSwitch = () => <ThemeSwitch />;

MyThemeSwitch.displayName = 'MyThemeSwitch';
```

## Live Preview

Here you can see the **ThemeSwitch** component in action:

<Preview withSource="none">
  <Story name="Get started" decorators={[addThemeProvider(theme)]}>
    <div>
      <Button variant="contained">This is button</Button>
      <ThemeSwitch />
    </div>
  </Story>
</Preview>

## ThemeSwitch Props

Here you can see the props for the **ThemeSwitch** component:

<Props of={ThemeSwitch} />
