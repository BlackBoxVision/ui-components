import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import StoryRouter from 'storybook-react-router';
import { linkTo } from '@storybook/addon-links';
import { Route, Switch } from 'react-router-dom';

import { LogoComponent } from './utils/components/LogoComponent';
import { MenuComponent } from './utils/components/MenuComponent';
import { addThemeProvider } from './utils/decorators/theme';
import { menuItems } from './utils/items';

import { Header, lightTheme } from '../src';

<Meta title="UI Components/Header" component={Header} />

# Header

Stories related to **Header** component.

## Example

This is how you can use **Header** component:

```jsx
import React from 'react';
import { Header } from '@blackbox-vision/ui-components';

import LogoComponent from './LogoComponent';
import MenuComponent from './MenuComponent';
import { menuItems } from './utils/items';

export const MyHeader = () => (
  <Header>
    <LogoComponent />
    <MenuComponent menu={menuItems} />
  </Header>
);

MyHeader.displayName = 'MyHeader';
```

## Live Preview

Here you can see the **Header** component in action:

<Preview withSource="none">
  <Story
    name="Get started"
    decorators={[
      addThemeProvider(lightTheme),
      StoryRouter(
        {
          '/': linkTo('Header', 'home'),
        },
        { initialEntries: ['/about'] }
      ),
    ]}
  >
    <Switch>
      <Route exact path="/">
        <Header>
          <LogoComponent />
          <MenuComponent menu={menuItems} />
        </Header>
        <p style={{ margin: 0, paddingTop: 80 }}>I'm in the "Home" page</p>
      </Route>
      <Route exact path="/about">
        <Header>
          <LogoComponent />
          <MenuComponent menu={menuItems} />
        </Header>
        <p style={{ margin: 0, paddingTop: 80 }}>I'm in the "About" page</p>
      </Route>
    </Switch>
  </Story>
</Preview>

## Header Props

Here you can see the props for the **Header** component:

<Props of={Header} />
